# FA CS Automator - Environment Configuration
# Copy this file to .env and fill in your values

# OpenAI API Key (Required for GPT classification and embeddings)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here

# Optional: Specify the model for classification (default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini

# Optional: Specify embedding model for memory engine (default: text-embedding-3-small)
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small
