# ==============================================================================
# FA CS Automator - Environment Configuration
# ==============================================================================
# SETUP:
#   1. Copy this file: cp .env.example .env
#   2. Add your OpenAI API key below
#   3. Run: docker-compose up
# ==============================================================================

# OpenAI API Key (Required for AI classification)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here

# OpenAI Model (optional, default: gpt-4o-mini)
# Options: gpt-4o-mini (fast/cheap), gpt-4o (best), gpt-4-turbo
OPENAI_MODEL=gpt-4o-mini

# Embedding model for memory engine (optional)
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Log Level (optional, default: INFO)
# Options: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# ==============================================================================
# AWS S3 Configuration (Optional - for remote tax rules updates)
# ==============================================================================
# Enable S3-based tax rules configuration for centralized updates.
# When configured, tax rules are fetched from S3 with local caching.
# Fallback: local file (backend/config/tax_rules.json) -> embedded defaults

# S3 bucket name containing tax_rules.json
# TAX_RULES_S3_BUCKET=fa-cs-automator-config-prod

# S3 object key (default: tax_rules.json)
# TAX_RULES_S3_KEY=tax_rules.json

# AWS region (default: ap-northeast-2)
# TAX_RULES_S3_REGION=ap-northeast-2

# Cache TTL in seconds (default: 3600 = 1 hour)
# TAX_RULES_CACHE_TTL_SECONDS=3600

# AWS Credentials (if not using IAM roles or instance profiles)
# Recommended: Use IAM roles for EC2/ECS instead of hardcoding credentials
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
