# FA CS Automator - Python Dependencies
# Updated: 2025-12-02 - Force cache rebuild

# Core Framework
fastapi>=0.100.0
uvicorn[standard]>=0.22.0

# Data Processing
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0

# API & Utilities
openai>=1.0.0
python-dotenv>=1.0.0
python-multipart>=0.0.6
psutil>=5.9.0

# Text Matching & Similarity
rapidfuzz>=3.7.0

# Visualization
plotly>=5.19.0

# Security & Authentication
pydantic>=2.0.0
PyJWT>=2.8.0          # JWT token handling for authentication

# AWS S3 for remote configuration
boto3>=1.34.0         # AWS SDK for S3 tax rules config

# Optional: Redis for scalable session storage
# redis>=5.0.0

# Optional: For database encryption
# cryptography>=41.0.0

# Note: RPA libraries (pyautogui, pywinauto, etc.) are Windows-only
# For RPA features on Windows, see requirements-rpa.txt

# Optional: Uncomment for semantic similarity features
# sentence-transformers
# torch
